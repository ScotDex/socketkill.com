services:
  killstream-bot:
    build: .
    container_name: kill-stream-bot
    restart: always
    # Automatically loads variables from your .env file
    env_file:
      - .env
    # Maps the health check port to your Droplet's public port
    ports:
      - "8099:8099"
    # Setting resource limits prevents the bot from eating all Droplet RAM
    volumes:
      - "./data:/usr/src/app/data"
    deploy:
      resources:
        limits:
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"